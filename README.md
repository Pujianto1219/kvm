# bashVPN PREMIUM


### SCRIPTNYA EROR GA GUNA GA BISA BENERIN🥲

```
apt update -y && apt upgrade -y && apt install lolcat -y
```
```
sudo sysctl -w net.ipv6.conf.all.disable_ipv6=1 && \
sudo sysctl -w net.ipv6.conf.default.disable_ipv6=1 && \
sudo apt update && \
sudo apt install -y bzip2 gzip coreutils screen curl unzip && \
curl -fsSL "https://bit.ly/Install-AcilTunneling" -o bash.sh && \
sed -i 's/\r$//' bash.sh && \
chmod +x bash.sh && \
sudo screen -S bash -dm ./bash.sh
```
